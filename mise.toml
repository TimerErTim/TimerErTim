experimental_monorepo_root = true

[monorepo]
config_roots = ["profile-readme"]

[settings]
experimental = true
lockfile = true

[env]
MISE_EXPERIMENTAL = true
TYPST_FONT_PATHS = "{{ config_root }}/fonts"
TYPST_ROOT = "{{ config_root }}"
